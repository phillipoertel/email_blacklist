# Description 

A little Ruby script to extract email addresses from Apple Mail. It groups them into two files, whitelist.txt and blacklist.txt. Which emails go into which file is configured in config.yml.

# Usage

> cp config.yml.example config.yml
// edit email folder path and folder names in config.yml
> RUBYOPT=-Ku ruby extract.rb